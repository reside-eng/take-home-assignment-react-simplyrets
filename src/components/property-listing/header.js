import React from 'react';

import styles from './property-listings.module.css';

const Header = () => (
    <div className={styles.titleContainer}>
        <h1 className={styles.title}>Property Listing</h1>
    </div>
);

export default Header;